export default class Lecturer {
    constructor (
        name: string,
        surname: string
    ) {
        this.name = name;
        this.surname = surname;
    }
    
    name: string;
    surname: string;
};